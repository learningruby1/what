.container
  .col-md-6
    %fieldset
      %legend
        Resend unlock instructions
        %span.spain Reenviar desbloquear instrucciones

      = devise_error_messages!

      = form_for(resource, :as => resource_name, :url => unlock_path(resource_name), :html => { :method => :post }) do |f|
        .form-group
          = f.email_field :email, :autofocus => true, :placeholder => 'Email address / Correo Electrónico', :class => 'form-control'
        .form-group
          = f.submit "Resend unlock instructions / Reenviar desbloquear instrucciones", :class => 'btn btn-success btn-block'
        .form-group
          = render "devise/shared/links"