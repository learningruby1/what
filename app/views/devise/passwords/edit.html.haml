.container
  .col-md-6
    %fieldset
      %legend
        Change password
        %span.spain Cambiar la contraseña

      = devise_error_messages!

      .form-group
        = form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :put }) do |f|
          = f.hidden_field :reset_password_token
          .form-group
            = f.password_field :password, :placeholder => "Password / Contraseña", :autofocus => true, :class => 'form-control'
          .form-group
            = f.password_field :password_confirmation, :placeholder => "Password confirm / Confirmar contraseña", :class => 'form-control'
          .form-group
            = f.submit "Change my password / Cambiar mi contraseña", :class => 'btn btn-default btn-block'
          .form-group
            = render "devise/shared/links"