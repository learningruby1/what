.jumbotron
  .container
    %h2 Forms Mama
    %p A unique online multilingual Self-Help forms site
    %p.spain Un sitio único y multilingüe de solicitudes de auto ayuda
    - if @documents.present?
      %h2.text-muted
        - if user_signed_in?
          Welcome Back /
          %span.spain Gracias por volver
      - @documents.each do |document|
        = link_to document_answer_path(document, 1) do
          = document.to_s.split('<spain/>').first
          %span.spain= document.to_s.split('<spain/>').last
        %br

.container
  - @templates.each_with_index do |template, index|
    - if index % 3 == 0
      .row
    .col-md-4
      %h3
        = template.to_s.split('/<spain/>').first
        %span.spain= template.to_s.split('/<spain/>').last
      %p= template.to_desc.split('/<spain/>').first
      %p.spain= template.to_desc.split('/<spain/>').last
      %p= link_to 'Start new >>', template_documents_path(template), :class => 'btn btn-default', :method => :post